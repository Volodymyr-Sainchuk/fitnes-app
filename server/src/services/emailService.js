// Safe scaffold for sending emails. Replace with real email provider logic.
export async function sendEmail(to, subject, text) {
  // TODO: integrate with SendGrid, SES, SMTP, etc.
  console.log("sendEmail scaffold:", { to, subject });
  return true;
}
